# 2026-09-23

## What happened

A full day on the personal site, in four rounds, each one built by Claude Code and checked
from this side against a fresh production build before being called done.

- Early morning: brought in a complete design spec written from canvas mockups, a site
  built around an illustrated shepherd (Umwayi means shepherd), with daisyUI 5 components,
  exact copy, and two themes, pasture and nightwatch. It replaced both of yesterday's
  revisions rather than extending them. Before writing anything, the stack upgrade it needed
  (Tailwind 4, daisyUI 5) was tested against the real repository: a working copy built all
  13 pages. Claude Code then built it (`3b884cc`) and reconciled it against the real
  reference implementation the mockups came from (`7f4ac16`).
- Mid-morning: asked for all site content to live in this hub instead of the site's repo, so
  it can be read and updated here. A new `site/` folder now holds every article, project
  page, illustration, the profile photo, the Now section, and the profile links. Every
  article and project page was split into sections for the "On this page" list, and each
  ends with a "What I took from it" list. Ten illustrations were drawn in the redesign's
  style, with the generator kept in `site/tools/` so every new piece gets one. A fourth
  project page was added for the site itself. The live build was checked first and eight
  real gaps against the reference were found by measuring, not by reading the code:
  headings in the wrong font, code panel corners, an underline that never went away, two
  buttons with no hover, a card lift too small to see, the old favicon, a slim project
  template, and an empty duplicate comments heading. All went to Claude Code with the
  content move (`8066b2b`).
- Early afternoon: two more layout bugs reported and root-caused before anything was
  written for Claude Code. On screens wider than 1440px the article illustration stayed
  stuck to the left. An unlayered CSS reset was overriding the class meant to center it. The
  takeaway list broke around inline code like `UNKNOWN`, because each list item was a flex
  row that turned every text run into its own column. Both fixes were tested in the live
  page first. The settlement-engine repository was made public and linked. Hashnode, dev.to,
  Medium, and LinkedIn profiles now exist and are linked from the footer. The footer email
  sign-up now saves to Waline, with the address kept in Waline's private field only. Claude
  Code checked that live: the address never comes back from the public API. It also found
  the sign-up script had been silently missing from every build since the redesign, so the
  form had never actually worked until now (`262e7df`).
- Late afternoon: set a new rule. Nothing on the site names companies applied or interviewed
  to. The site's one instance, in the "What Becomes True When Both Stay" article, now says
  "a job interview". The homepage's open threads read straight from `career-prep.md`, and one
  of those bullets named a company too, so it was reworded at the source. Seven new journal
  excerpts were drafted for the site from 09-18 to today, with anything private left out.

## What I noticed / felt / thought it meant

- What I know: every round's bugs were found by building, opening, and measuring the real
  pages, never by reading the source. That held for design (the flat mascot yesterday),
  layout (two CSS reset bugs today), and behavior (a sign-up form that had never worked).
- What I know: the hub is now the single place the site's words and pictures come from. A
  push to `site/` rebuilds the site.
- What I am assuming: nothing new about Miguel himself. This entry is about the work.
- What remains unknown: whether the drafted "What I took from it" lists and the new excerpts
  read as his own once he reads them. They're drafts until he says so.

## What this might feed

`site/` (new excerpts, one reworded article), `projects/career-prep.md` (one open-question
bullet reworded), `MASTER_CONTEXT.md` (the no-company-names rule), `site/README.md` (the same
rule, for anyone writing site content), `backlog.md`. Logged in `SYNC_LOG.md`.

## Carrying forward

- What happened: the site moved to a new design, its content moved into this hub, and a
  privacy line was drawn for what the site says.
- What I learned: looking at the rendered result keeps catching what the code hides. Twice
  today the cause was the same one: a CSS reset outside any layer, beating the classes
  meant to override it.
- What remains uncertain: how new-article emails will actually reach the people who sign up.
  For now the form only collects addresses.
- What, if anything, I want to do next: read through the drafted takeaways and excerpts, then
  back to settlement-engine, outbox.